<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "DropChart" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "DropChart" %>">
    
    <script type="text/javascript" src="https://code.jquery.com/jquery-1.11.3.min.js"></script>
    
    <%= stylesheet_link_tag 'cosmo', media: 'all' %>
    <!-- <link type="text/css" href="/assets/jplayer.blue.monday.scss" rel="stylesheet" /> -->
    <%= javascript_include_tag 'cosmo' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
       <%= yield %>
  </body>
</html>
